# ── Application ──────────────────────────────────────
PORT=3000
SESSION_SECRET=change-me-to-a-random-64-char-string

# ── Database ─────────────────────────────────────────
# Option 1 (default): Use an external / hosted PostgreSQL database.
DATABASE_URL=postgresql://user:password@your-host:5432/backslash

# Option 2: Use the built-in Docker Compose PostgreSQL.
#            Uncomment the line below and start with:
#            docker compose --profile postgres up -d
# DATABASE_URL=postgresql://backslash:backslash@postgres:5432/backslash

# ── Compilation ──────────────────────────────────────
MAX_CONCURRENT_BUILDS=5
COMPILE_TIMEOUT=120
COMPILE_MEMORY=1g
COMPILE_CPUS=1.5

# ── Auth ─────────────────────────────────────────────
DISABLE_SIGNUP=false
